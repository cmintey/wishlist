services:
    app:
        container_name: wishlist
        image: wishlist:test
        network_mode: host
        volumes:
            - uploads:/usr/src/app/uploads
            - data:/usr/src/app/data
        environment:
            ORIGIN: http://localhost:3280

volumes:
    uploads:
    data:
